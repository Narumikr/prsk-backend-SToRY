# api/openapi.yaml
openapi: '3.0.3'

info:
  title: 'prsk Music API'
  description: |
    ## Feature
    - Get Hello SEKAI !
    - Get prsk music list
  version: '0.0.0'

tags:
  - name: Sample
    description: Trial creation API

paths:
  '/hello-sekai':
    get:
      operationId: getHelloSekai
      summary: 'Sample API for my learing'
      description: 'Sample API for my learning'
      tags: ['Sample']
      deprecated: false
      responses:
        '200':
          description: 'Success operation'
          content:
            application/json:
              schema:
                $ref: './schemas/hello-sekai.yaml#/components/schemas/HelloSekaiResponse'
